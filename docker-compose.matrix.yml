services:
  synapse:
    image: matrixdotorg/synapse:latest
    container_name: agent-im-synapse
    restart: unless-stopped
    environment:
      SYNAPSE_SERVER_NAME: localhost
      SYNAPSE_REPORT_STATS: "no"
    ports:
      - "8008:8008"
    volumes:
      - ./synapse-data:/data
